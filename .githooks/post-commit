#!/bin/sh
set -eu
if [ "${SKIP_DEPLOY:-}" = "1" ] || [ "$(git config --local --get synopsis.autoDeploy || true)" != "true" ]; then
  exit 0
fi
printf '%s\n' "提交成功，开始部署到本地配置指定的 Cloudflare Worker。"
pnpm run deploy
